# ==============================================================================
# DEPENDENCIAS DEL PROYECTO - RIQCHARIY FINANZAS BACKEND
# ==============================================================================
# Este archivo lista todas las dependencias Python necesarias para el backend.
# 
# Para instalar todas las dependencias:
#   pip install -r requirements.txt
#
# Para agregar nuevas dependencias:
#   1. Instalar con: pip install <nombre-paquete>
#   2. Agregar a este archivo con la versión específica
# ==============================================================================

# ------------------------------------------------------------------------------
# FRAMEWORK WEB
# ------------------------------------------------------------------------------
fastapi>=0.109.0           # Framework web asíncrono de alto rendimiento
uvicorn[standard]>=0.27.0  # Servidor ASGI para ejecutar FastAPI

# ------------------------------------------------------------------------------
# VALIDACIÓN Y CONFIGURACIÓN
# ------------------------------------------------------------------------------
pydantic>=2.5.0            # Validación de datos y serialización
pydantic-settings>=2.1.0   # Manejo de configuración desde .env
python-dotenv>=1.0.0       # Carga de variables de entorno desde .env

# ------------------------------------------------------------------------------
# AUTENTICACIÓN Y SEGURIDAD
# ------------------------------------------------------------------------------
python-jose[cryptography]>=3.3.0  # Manejo de tokens JWT
passlib[bcrypt]>=1.7.4            # Hashing seguro de contraseñas
python-multipart>=0.0.6           # Soporte para form-data (login)

# ------------------------------------------------------------------------------
# CLIENTE HTTP (Para integraciones externas)
# ------------------------------------------------------------------------------
httpx>=0.26.0              # Cliente HTTP asíncrono (para llamadas a APIs externas)

# ------------------------------------------------------------------------------
# UTILIDADES
# ------------------------------------------------------------------------------
email-validator>=2.1.0     # Validación de emails en schemas

# ------------------------------------------------------------------------------
# TESTING (Descomentar cuando se necesiten pruebas)
# ------------------------------------------------------------------------------
# pytest>=7.4.0            # Framework de testing
# pytest-asyncio>=0.23.0   # Soporte async para pytest
# httpx>=0.26.0            # Cliente HTTP para tests

# ------------------------------------------------------------------------------
# BASE DE DATOS (Descomentar cuando se configure la BD)
# ------------------------------------------------------------------------------
# sqlalchemy>=2.0.25       # ORM para base de datos
# asyncpg>=0.29.0          # Driver async para PostgreSQL
# alembic>=1.13.0          # Migraciones de base de datos
